{
    "agent-namespace": "healthcare-data",
    "agent-namespace-desc": "A namespace for the Synthea healthcare data agents configuration.",
    "agent-config": [
        {
            "agent-name": "synthea-healthcare-agent",
            "agent-version": "0.1.0",
            "agent-description": "An agent that queries Synthea healthcare data through Microsoft Fabric.",
            "toolset": [ "fabric_synthea_data" ],
            "prompt": "You are a healthcare data analyst agent that queries Synthea patient data through Microsoft Fabric. Use the Fabric tool to answer questions about patient demographics, conditions, medications, encounters, and procedures. Format all data responses as markdown tables."
        }
    ]
}
